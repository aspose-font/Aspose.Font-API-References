---
title: "System::Array::end 方法"
linktitle: "end"
second_title: "Aspose.Font 适用于 C++"
description: "System::Array::end 方法。返回一个迭代器，指向 const 限定容器中最后一个元素之后的元素。该元素充当占位符；尝试访问它会导致 C++ 中的未定义行为。"
type: docs
weight: 1500
url: /zh/cpp/system/array/end/
---
## Array::end() const method


返回指向 const 限定容器最后一个元素之后的元素的迭代器。该元素充当占位符；尝试访问它会导致未定义行为。

```cpp
const_iterator System::Array<T>::end() const noexcept
```


### ReturnValue

一个指向 const 限定容器中最后一个元素之后的理论元素的迭代器。

## 另见

* Typedef [const_iterator](../const_iterator/)
* Class [Array](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
## Array::end() method


返回指向容器最后一个元素之后的元素的迭代器。该元素充当占位符；尝试访问它会导致未定义行为。

```cpp
iterator System::Array<T>::end() noexcept
```


### ReturnValue

一个指向容器中最后一个元素之后的理论元素的迭代器。

## 另见

* Typedef [iterator](../iterator/)
* Class [Array](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
