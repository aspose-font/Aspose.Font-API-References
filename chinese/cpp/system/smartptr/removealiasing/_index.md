---
title: "System::SmartPtr::RemoveAliasing 方法"
linktitle: "RemoveAliasing"
second_title: "Aspose.Font 适用于 C++"
description: "System::SmartPtr::RemoveAliasing 方法。移除指针的别名（由别名构造函数创建），确保它在 C++ 中管理（如果是共享）或跟踪（如果是弱引用）指向的同一对象。"
type: docs
weight: 3100
url: /zh/cpp/system/smartptr/removealiasing/
---
## SmartPtr::RemoveAliasing method


从指针中移除别名（由别名构造函数创建），确保它管理（如果是共享）或跟踪（如果是弱引用）指向的同一对象。

```cpp
SmartPtr_ System::SmartPtr<T>::RemoveAliasing() const
```


### ReturnValue

指向同一对象的指针，该指针负责该对象的生命周期管理。

## 另见

* Typedef [SmartPtr_](../smartptr_/)
* Class [SmartPtr](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
