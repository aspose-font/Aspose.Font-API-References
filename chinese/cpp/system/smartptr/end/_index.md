---
title: "System::SmartPtr::end 方法"
linktitle: "end"
second_title: "Aspose.Font 适用于 C++"
description: "System::SmartPtr::end 方法。用于访问底层集合的 end() 方法。仅在 SmartPtr_ 为具有 end() 方法的特化类型时可编译（C++）。"
type: docs
weight: 900
url: /zh/cpp/system/smartptr/end/
---
## SmartPtr::end() method


用于访问底层集合的 [end()](./) 方法的访问器。仅在 [SmartPtr_](../smartptr_/) 为具有 [end()](./) 方法的特化类型时可编译。

```cpp
template<typename Q> decltype(std::declval<Q>().end()) System::SmartPtr<T>::end()
```


### ReturnValue

指向集合末尾的迭代器

## 另见

* Class [SmartPtr](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
## SmartPtr::end() const method


用于访问底层集合的 [end()](./) 方法的访问器。仅在 [SmartPtr_](../smartptr_/) 为具有 [end()](./) 方法的特化类型时可编译。

```cpp
template<typename Q> decltype(std::declval<const Q>().end()) System::SmartPtr<T>::end() const
```


### ReturnValue

指向集合末尾的迭代器

## 另见

* Class [SmartPtr](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
