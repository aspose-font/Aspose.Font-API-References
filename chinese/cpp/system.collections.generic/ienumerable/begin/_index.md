---
title: "System::Collections::Generic::IEnumerable::begin method"
linktitle: "begin"
second_title: "Aspose.Font 适用于 C++"
description: "System::Collections::Generic::IEnumerable::begin 方法。获取指向集合中第一个元素（如果有）的迭代器。由于 GetEnumerator() 在 C++ 中返回 T 的拷贝对象，不能使用此迭代器来修改被引用的对象。"
type: docs
weight: 100
url: /zh/cpp/system.collections.generic/ienumerable/begin/
---
## IEnumerable::begin() method


获取指向集合中第一个元素（如果有）的迭代器。由于 [GetEnumerator()](../getenumerator/) 返回 T 的拷贝对象，不能使用此迭代器来修改被引用的对象。

```cpp
iterator System::Collections::Generic::IEnumerable<T>::begin()
```


### ReturnValue

指向集合中第一个元素（如果有）的迭代器

## 另见

* Typedef [iterator](../iterator/)
* Class [IEnumerable](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Font for C++](../../../)
## IEnumerable::begin() const method


获取指向集合的 const 限定实例中第一个元素（如果存在）的迭代器。

```cpp
const_iterator System::Collections::Generic::IEnumerable<T>::begin() const
```


### ReturnValue

指向集合的 const 限定实例中第一个元素（如果有）的迭代器

## 另见

* Typedef [const_iterator](../const_iterator/)
* Class [IEnumerable](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Font for C++](../../../)
