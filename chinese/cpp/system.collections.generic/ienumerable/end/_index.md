---
title: "System::Collections::Generic::IEnumerable::end 方法"
linktitle: "end"
second_title: "Aspose.Font 适用于 C++"
description: "System::Collections::Generic::IEnumerable::end 方法。获取指向集合中最后一个元素之后的位置的迭代器（如果有）。由于 GetEnumerator() 在 C++ 中返回 T 的拷贝对象，此迭代器无法用于更改被引用的对象。"
type: docs
weight: 400
url: /zh/cpp/system.collections.generic/ienumerable/end/
---
## IEnumerable::end() method


获取指向集合中最后一个元素之后的位置的迭代器（如果有）。由于 [GetEnumerator()](../getenumerator/) 返回 T 的拷贝对象，此迭代器无法用于更改被引用的对象。

```cpp
iterator System::Collections::Generic::IEnumerable<T>::end()
```


### ReturnValue

指向集合中最后一个元素之后的位置的迭代器（如果有）

## 另见

* Typedef [iterator](../iterator/)
* Class [IEnumerable](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Font for C++](../../../)
## IEnumerable::end() const method


获取指向集合的 const 限定实例中最后一个元素之后的位置的迭代器（如果存在）。

```cpp
const_iterator System::Collections::Generic::IEnumerable<T>::end() const
```


### ReturnValue

指向集合中 const 限定实例的最后一个元素之后的位置的迭代器（如果有）

## 另见

* Typedef [const_iterator](../const_iterator/)
* Class [IEnumerable](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.Font for C++](../../../)
