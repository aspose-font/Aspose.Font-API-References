---
title: "Espacio de nombres System::ComponentModel"
linktitle: "System::ComponentModel"
second_title: "Aspose.Font para C++"
description: "Cómo usar el espacio de nombres System::ComponentModel en C++."
type: docs
weight: 3100
url: /es/cpp/system.componentmodel/
---



## Clases

| Clase | Descripción |
| --- | --- |
| [AsyncCompletedEventArgs](./asynccompletedeventargs/) | Una instancia de esta clase se pasa como argumento al delegado AsyncCompletedEventHandler. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [BackgroundWorker](./backgroundworker/) | Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [CancelEventArgs](./canceleventargs/) | Argumentos del evento cancelable. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [Component](./component/) | Clase ficticia para que el código traducido que usa la clase [Component](./component/) sea compilable. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [Container](./container/) | Clase ficticia para que el código traducido que usa la clase [Container](./container/) funcione. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [DoWorkEventArgs](./doworkeventargs/) | Argumentos del evento DoWork. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [EnumConverter](./enumconverter/) | Clase ficticia para que el código traducido que usa EnumConverter compile. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [IComponent](./icomponent/) | Clase ficticia para que el código que usa IComponent compile después de traducir. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [IContainer](./icontainer/) | Interfaz ficticia para que el código que usa IContainer compile. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [ITypeDescriptorContext](./itypedescriptorcontext/) | Interfaz ficticia para que el código que usa ITypeDescriptorContext compile. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [ProgressChangedEventArgs](./progresschangedeventargs/) | Una instancia de esta clase se pasa como argumento al delegado ProgressChangedEventHandler. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [PropertyChangedEventArgs](./propertychangedeventargs/) | Argumentos del evento PropertyChanged. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [RunWorkerCompletedEventArgs](./runworkercompletedeventargs/) | Una instancia de esta clase se pasa como argumento al delegado RunWorkerCompletedEventHandler. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [TypeConverter](./typeconverter/) | Clase que maneja la conversión de tipos en el modelo de componentes. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
| [TypeDescriptor](./typedescriptor/) | Clase ficticia para que el código que usa TypeDescriptor compile después de traducir. Los objetos de esta clase solo deben asignarse mediante la función [System::MakeObject()](../system/makeobject/). Nunca cree una instancia de este tipo en la pila ni usando el operador new, ya que provocará errores de tiempo de ejecución y/o fallos de aserción. Siempre envuelva esta clase en un puntero [System::SmartPtr](../system/smartptr/) y use ese puntero para pasarlo a las funciones como argumento. |
## Typedefs

| Typedef | Descripción |
| --- | --- |
| [DoWorkEventHandler](./doworkeventhandler/) | Tipo de manejador para el evento DoWork. |
| [PropertyChangedEventHandler](./propertychangedeventhandler/) | Tipo de manejador para el evento PropertyChanged. |
| [Win32Exception](./win32exception/) |  |
