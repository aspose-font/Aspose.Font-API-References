---
title: "System::Char::IsUpper méthode"
linktitle: "IsUpper"
second_title: "Aspose.Font pour C++"
description: "System::Char::IsUpper méthode. Détermine si le caractère spécifié est classé comme une lettre majuscule en C++."
type: docs
weight: 1900
url: /fr/cpp/system/char/isupper/
---
## Char::IsUpper(char_t) method


Détermine si le caractère spécifié est classé comme une lettre majuscule.

```cpp
static bool System::Char::IsUpper(char_t c)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| c | char_t | Le caractère à tester |

### ReturnValue

Vrai si le caractère spécifié est une lettre majuscule, sinon - false

## Voir aussi

* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
## Char::IsUpper(const char_t *, int) method


Détermine si le caractère à l'index spécifié dans le tampon de caractères spécifié est classé comme une lettre majuscule.

```cpp
static bool System::Char::IsUpper(const char_t *str, int idx)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| str | const char_t * | Pointeur vers le début du tampon de caractères |
| idx | int | Un indice basé sur zéro dans le tampon spécifié du caractère à tester |

### ReturnValue

Vrai si le caractère à l'index spécifié est une lettre majuscule, sinon - false

## Voir aussi

* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
## Char::IsUpper(const String\&, int) method


Détermine si le caractère à l'index spécifié dans la chaîne spécifiée est classé comme une lettre majuscule.

```cpp
static bool System::Char::IsUpper(const String &str, int idx)
```


| Paramètre | Type | Description |
| --- | --- | --- |
| str | const String\& | La chaîne contenant un caractère. |
| idx | int | Un index basé sur zéro dans la chaîne spécifiée à tester |

### ReturnValue

Vrai si le caractère à l'index spécifié est une lettre majuscule, sinon - false

## Voir aussi

* Class [String](../../string/)
* Class [Char](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
