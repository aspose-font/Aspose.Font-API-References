---
title: "System::SmartPtr::end metodo"
linktitle: "end"
second_title: "Aspose.Font per C++"
description: "System::SmartPtr::end metodo. Accessorio per il metodo end() di una collezione sottostante. Compila solo se SmartPtr_ è un tipo specializzato con il metodo end() in C++."
type: docs
weight: 900
url: /it/cpp/system/smartptr/end/
---
## SmartPtr::end() method


Accessor per il metodo [end()](./) di una collezione sottostante. Compila solo se [SmartPtr_](../smartptr_/) è un tipo specializzato con il metodo [end()](./).

```cpp
template<typename Q> decltype(std::declval<Q>().end()) System::SmartPtr<T>::end()
```


### ReturnValue

iteratore alla fine della collezione

## Vedi anche

* Class [SmartPtr](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
## SmartPtr::end() const method


Accessor per il metodo [end()](./) di una collezione sottostante. Compila solo se [SmartPtr_](../smartptr_/) è un tipo specializzato con il metodo [end()](./).

```cpp
template<typename Q> decltype(std::declval<const Q>().end()) System::SmartPtr<T>::end() const
```


### ReturnValue

iteratore alla fine della collezione

## Vedi anche

* Class [SmartPtr](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
