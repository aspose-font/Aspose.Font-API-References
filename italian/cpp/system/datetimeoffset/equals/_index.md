---
title: "Metodo System::DateTimeOffset::Equals"
linktitle: "Uguali"
second_title: "Aspose.Font per C++"
description: "Metodo System::DateTimeOffset::Equals. Verifica se due oggetti DateTimeOffset rappresentano lo stesso istante temporale in C++."
type: docs
weight: 1200
url: /it/cpp/system/datetimeoffset/equals/
---
## DateTimeOffset::Equals(const DateTimeOffset\&) const method


Verifica se due oggetti [DateTimeOffset](../) rappresentano lo stesso istante temporale.

```cpp
bool System::DateTimeOffset::Equals(const DateTimeOffset &other) const
```


| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| other | const DateTimeOffset\& | [Object](../../object/) da confrontare. |

### ReturnValue

true se l'oggetto corrente è uguale a **other**, altrimenti - false.

## Vedi anche

* Class [DateTimeOffset](../)
* Class [DateTimeOffset](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
## DateTimeOffset::Equals(const SharedPtr\<Object\>\&) const method


Verifica se due oggetti [DateTimeOffset](../) rappresentano lo stesso istante temporale.

```cpp
bool System::DateTimeOffset::Equals(const SharedPtr<Object> &obj) const
```


| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| obj | const SharedPtr\<Object\>\& | [Object](../../object/) da confrontare. |

### ReturnValue

true se l'oggetto corrente è uguale a **obj**, altrimenti - false.

## Vedi anche

* Typedef [SharedPtr](../../sharedptr/)
* Class [Object](../../object/)
* Class [DateTimeOffset](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
## DateTimeOffset::Equals(const DateTimeOffset\&, const DateTimeOffset\&) method


Verifica se due oggetti [DateTimeOffset](../) rappresentano lo stesso istante temporale.

```cpp
static bool System::DateTimeOffset::Equals(const DateTimeOffset &first, const DateTimeOffset &second)
```


| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| primo | const DateTimeOffset\& | Primo oggetto da confrontare. |
| secondo | const DateTimeOffset\& | Secondo oggetto da confrontare. |

### ReturnValue

vero se **first** è uguale a **second**, altrimenti - falso.

## Vedi anche

* Class [DateTimeOffset](../)
* Class [DateTimeOffset](../)
* Namespace [System](../../)
* Library [Aspose.Font for C++](../../../)
