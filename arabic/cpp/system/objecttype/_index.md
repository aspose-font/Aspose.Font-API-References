---
title: "فئة System::ObjectType"
linktitle: "ObjectType"
second_title: "Aspose.Font لـ C++"
description: "فئة System::ObjectType. توفر طرقًا ثابتة تنفّذ getters لنوع الكائن. هذا نوع ثابت لا يحتوي على خدمات مثيل. يجب ألا تقوم بإنشاء مثيلات منه بأي وسيلة في C++."
type: docs
weight: 5000
url: /ar/cpp/system/objecttype/
---
## ObjectType class


يوفر طرقاً ثابتة تنفّذ getters لأنواع الكائنات. هذا نوع ثابت لا يحتوي على خدمات مثيل. لا يجب أبداً إنشاء أمثلة منه بأي وسيلة.

```cpp
class ObjectType
```

## الطرق

| طريقة | الوصف |
| --- | --- |
| static [GetType](./gettype/)(const T\&) | يُنفّذ ترجمة typeof(). تحميل زائد للمؤشرات الذكية. |
| static [GetType](./gettype/)(const T\&) | يُنفّذ ترجمة typeof(). تحميل زائد للهياكل. |
| static [GetType](./gettype/)(const T\&) | يُنفّذ ترجمة typeof(). تحميل زائد للاستثناءات. |
| static [GetType](./gettype/)(const T) | يُنفّذ ترجمة typeof(). تحميل زائد للأنواع الأولية. |
| static [GetType](./gettype/)(const T) | يُنفّذ ترجمة typeof(). تحميل زائد لأنواع [Nullable](../nullable/). |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد للأنواع الأولية. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لأنواع التعداد. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد للهياكل والمؤشرات. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لـ[Nullable](../nullable/). |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لـMutlicastDelegate. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد للهياكل والمؤشرات. |
| static [GetType](./gettype/)(const String\&) | يُنفّذ ترجمة typeof(). تحميل زائد لنوع السلسلة. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لـuint8_t. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لـuint8_t. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لـuint8_t. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لـuint8_t. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لـuint8_t. |
| static [GetType](./gettype/)() | يُنفّذ ترجمة typeof(). تحميل زائد لـuint8_t. |
## انظر أيضًا

* Namespace [System](../)
* Library [Aspose.Font for C++](../../)
