---
title: "System::Drawing::RectangleF فئة"
linktitle: "RectangleF"
second_title: "Aspose.Font لـ C++"
description: "System::Drawing::RectangleF فئة. تمثل مساحة مستطيلة من صورة تُعرّف بإحداثيات X وY ذات دقة عائمة أحادية للزاوية العلوية اليسرى وعرضها وارتفاعها. يجب تخصيص هذا النوع على المكدس وتمريره إلى الدوال بالقيمة أو بالمرجع. لا تستخدم فئة System::SmartPtr لإدارة كائنات هذا النوع في C++."
type: docs
weight: 2000
url: /ar/cpp/system.drawing/rectanglef/
---
## RectangleF class


يمثل مساحة مستطيلة من صورة تُعرّف بإحداثيات X وY ذات دقة عائمة أحادية للزاوية العلوية اليسرى وعرضها وارتفاعها. يجب تخصيص هذا النوع على المكدس وتمريره إلى الدوال بالقيمة أو بالمرجع. لا تستخدم فئة [System::SmartPtr](../../system/smartptr/) لإدارة كائنات هذا النوع.

```cpp
class RectangleF
```

## الطرق

| طريقة | الوصف |
| --- | --- |
| [Contains](./contains/)(float, float) | يحدد ما إذا كانت النقطة المحددة موجودة داخل المستطيل الذي يمثله الكائن الحالي. |
| [Contains](./contains/)(const PointF\&) | يحدد ما إذا كانت النقطة المحددة موجودة داخل المستطيل الذي يمثله الكائن الحالي. |
| [Contains](./contains/)(const RectangleF\&) | يحدد ما إذا كان المستطيل المحدد يقع داخل المستطيل الممثل بواسطة الكائن الحالي. |
| [Equals](./equals/)(const RectangleF\&) const | يحدد ما إذا كانت المستطيلات الممثلة بواسطة الكائن الحالي والكائن المحدد متطابقة. |
| static [FromLTRB](./fromltrb/)(float, float, float, float) | ينشئ كائنًا جديدًا من نوع [RectangleF](./) يمثل مستطيلًا بمواقع الحواف المحددة. |
| [get_Bottom](./get_bottom/)() const | يعيد إحداثي y للحافة السفلية للمستطيل الممثل بواسطة الكائن الحالي. |
| [get_Height](./get_height/)() const | يعيد ارتفاع المستطيل الممثل بواسطة الكائن الحالي. |
| [get_IsEmpty](./get_isempty/)() const | يحدد ما إذا كانت إحداثيات X وY للزاوية العلوية اليسرى للمستطيل الممثل بواسطة الكائن الحالي وكذلك عرضه وارتفاعه تساوي 0. |
| [get_Left](./get_left/)() const | يعيد إحداثي X للحافة اليسرى للمستطيل الممثل بواسطة الكائن الحالي. |
| [get_Location](./get_location/)() const | يعيد نسخة من فئة [PointF](../pointf/) التي تحدد موقع الزاوية العلوية اليسرى للمستطيل الممثل بواسطة الكائن الحالي. |
| [get_Right](./get_right/)() const | يعيد إحداثي X للحافة اليمنى للمستطيل الممثل بواسطة الكائن الحالي. |
| [get_Size](./get_size/)() const | يعيد نسخة من فئة [SizeF](../sizef/) التي تحدد عرض وارتفاع المستطيل الممثل بواسطة الكائن الحالي. |
| [get_Top](./get_top/)() const | يعيد إحداثي Y للحافة العلوية للمستطيل الممثل بواسطة الكائن الحالي. |
| [get_Width](./get_width/)() const | يعيد عرض المستطيل الممثل بواسطة الكائن الحالي. |
| [get_X](./get_x/)() const | يعيد إحداثي X للزاوية العلوية اليسرى للمستطيل الممثل بواسطة الكائن الحالي. |
| [get_Y](./get_y/)() const | يعيد إحداثي Y للزاوية العلوية اليسرى للمستطيل الممثل بواسطة الكائن الحالي. |
| [GetHashCode](./gethashcode/)() const | يعيد رمز تجزئة (hash code) للكائن الحالي. |
| [Inflate](./inflate/)(float, float) | يزيد عرض وارتفاع المستطيل الممثل بواسطة الكائن الحالي، مع الحفاظ على موقع المركز الهندسي للمستطيل. يتم زيادة العرض والارتفاع في الاتجاهين بالمقادير المحددة. |
| [Inflate](./inflate/)(const SizeF\&) | يزيد عرض وارتفاع المستطيل الممثل بواسطة الكائن الحالي، مع الحفاظ على موقع المركز الهندسي للمستطيل. يتم زيادة العرض والارتفاع في الاتجاهين بالمقادير المحددة بواسطة قيم العرض والارتفاع لكائن الحجم المحدد على التوالي. |
| static [Inflate](./inflate/)(const RectangleF\&, float, float) | يزيد عرض وارتفاع المستطيل الممثل بواسطة الكائن المحدد، مع الحفاظ على موقع المركز الهندسي للمستطيل. يتم زيادة العرض والارتفاع في الاتجاهين بالمقادير المحددة. |
| [Intersect](./intersect/)(const RectangleF\&) | يستبدل المستطيل الممثل بواسطة الكائن الحالي بالمستطيل الناتج عن تقاطعه مع المستطيل الممثل بواسطة الكائن المحدد. |
| static [Intersect](./intersect/)(const RectangleF\&, const RectangleF\&) | يعيد مستطيلًا يكون نتيجة تقاطع المستطيلات المحددة. |
| [IntersectsWith](./intersectswith/)(const RectangleF\&) | يحدد ما إذا كانت المستطيلات الممثلة بواسطة الكائن الحالي والكائن المحدد تتقاطع. |
| [Offset](./offset/)(const PointF\&) | يُزاح موقع المستطيل الممثل بواسطة الكائن الحالي بالمقادير المحددة. |
| [Offset](./offset/)(float, float) | يُزاح موقع المستطيل الممثل بواسطة الكائن الحالي بالمقادير المحددة. |
| [operator!=](./operator!=/)(std::nullptr_t) const | دائمًا تُعيد true. |
| [operator==](./operator==/)(std::nullptr_t) const | دائمًا تُعيد false. |
| [RectangleF](./rectanglef/)() | ينشئ نسخة جديدة من كائن [RectangleF](./) يمثل مستطيلًا بإحداثيات X وY وعرض وارتفاع مضبوطة على 0. |
| [RectangleF](./rectanglef/)(float, float, float, float) | ينشئ نسخة جديدة من كائن [RectangleF](./) يمثل مستطيلًا بالإحداثيات المحددة للزاوية العلوية اليسرى وعرض وارتفاع. |
| [RectangleF](./rectanglef/)(const PointF\&, const SizeF\&) | ينشئ نسخة جديدة من كائن [RectangleF](./) يمثل مستطيلًا بإحداثيات زاويته العلوية اليسرى المحددة كنسخة من فئة [PointF](../pointf/) وعرضه وارتفاعه كنسخة من فئة [SizeF](../sizef/). |
| explicit [RectangleF](./rectanglef/)(const Rectangle\&) | ينشئ نسخة جديدة من كائن [RectangleF](./) الذي يمثل المستطيل المكافئ للمستطيل المحدد. |
| [set_Height](./set_height/)(float) | يضبط ارتفاع المستطيل الممثل بالكائن الحالي. |
| [set_Location](./set_location/)(PointF) | يضبط موقع الزاوية العليا اليسرى للمستطيل الممثل بالكائن الحالي. |
| [set_Size](./set_size/)(SizeF) | يضبط العرض والارتفاع للمستطيل الممثل بالكائن الحالي. |
| [set_Width](./set_width/)(float) | يضبط عرض المستطيل الممثل بالكائن الحالي. |
| [set_X](./set_x/)(float) | يضبط إحداثي X للزاوية العليا اليسرى للمستطيل الممثل بالكائن الحالي. |
| [set_Y](./set_y/)(float) | يضبط إحداثي Y للزاوية العليا اليسرى للمستطيل الممثل بالكائن الحالي. |
| [ToString](./tostring/)() const | يرجع تمثيل النص للكائن الحالي. |
| static [Union](./union/)(const RectangleF\&, const RectangleF\&) | يرجع مستطيلًا يكون نتيجة اتحاد المستطيلات المحددة. |
## الحقول

| حقل | الوصف |
| --- | --- |
| static [Empty](./empty/) | مستطيل فارغ أي مستطيل تكون قيم الموقع والحجم فيه صفرًا. |
## انظر أيضًا

* Namespace [System::Drawing](../)
* Library [Aspose.Font for C++](../../)
